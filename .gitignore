# macOS
.DS_Store

# Editor / OS cruft
*.swp
*~
.vscode/

# LaTeX build artifacts (we commit resume.pdf + main.tex, not these)
resume-src/*.aux
resume-src/*.fdb_latexmk
resume-src/*.fls
resume-src/*.log
resume-src/*.out
resume-src/*.synctex.gz
resume-src/main.pdf
